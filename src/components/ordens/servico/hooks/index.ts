
export { useServicoTracker } from './useServicoTracker';
export type { ServicoStatus } from './types/servicoTrackerTypes';

// Este arquivo agora fornece apenas as exportações necessárias
// Isso garante que usamos as implementações corretas para os hooks do tracker de serviço
